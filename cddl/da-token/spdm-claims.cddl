spdm-claims = {
  &(measurements: 1) => { + block-id => spdm-measurement }
  &(certificates: 2) => spdm-certificates
  ? &(signature: 3) => spdm-measurement-blocks-signature
}

block-id = 0..254 ; TODO(mathieu) is the upper bound 250 instead?
